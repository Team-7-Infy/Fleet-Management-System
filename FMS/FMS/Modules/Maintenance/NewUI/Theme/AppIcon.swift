import Foundation

enum AppIcon {
    static let dashboard = FleetIcon.dashboard
    static let jobs = FleetIcon.jobs
    static let vehicle = FleetIcon.truck
    static let workOrder = FleetIcon.workOrder
    static let activity = FleetIcon.activity
    static let checkmark = FleetIcon.checkmark
    static let warning = FleetIcon.warning
    static let search = FleetIcon.search
    static let photo = FleetIcon.photo
    static let add = FleetIcon.add
}
