import SwiftUI

enum AppColor {
    static let brand = Color.blue
    static let background = Color(red: 0.94, green: 0.97, blue: 1.0) // Very light soft smooth blue
    static let secondaryBackground = Color(red: 0.97, green: 0.985, blue: 1.0) // Slightly lighter for contrast
    static let surface = Color.white // Whitest white
    static let textPrimary = Color.primary
    static let textSecondary = Color.secondary
    static let separator = Color(.separator)
    static let success = Color.green
    static let warning = Color.orange
    static let destructive = Color.red
}
