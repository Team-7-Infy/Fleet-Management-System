import SwiftUI

enum AppTypography {
    static let largeTitle = FleetTypography.screenTitle
    static let title = FleetTypography.sectionTitle
    static let headline = FleetTypography.cardTitle
    static let body = FleetTypography.body
    static let callout = FleetTypography.callout
    static let footnote = FleetTypography.footnote
    static let caption = FleetTypography.caption
}
